<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.mingle.pulltonextlayout.PullToNextLayout
        android:id="@+id/pulltonextlayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    </com.mingle.pulltonextlayout.PullToNextLayout>

    <Button
        android:id="@+id/to_top_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginRight="10dp"
        android:layout_marginBottom="10dp"
        android:text="到顶部"/>

    <Button
        android:id="@+id/delete_item_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toLeftOf="@id/to_top_btn"
        android:layout_alignBottom="@id/to_top_btn"
        android:layout_marginRight="10dp"
        android:text="删除条目"/>

</RelativeLayout>
